# Consonant Orchestra

Static GitHub Pages redesign of the Consonant Orchestra Carrd site.

## Local preview

Open `index.html` directly in a browser, or run:

```sh
python3 -m http.server 8000
```

Then visit `http://localhost:8000`.
